interface CloudUploadIconProps {
    fillColor?: string;
    strokeColor?: string;
    size?: number;
}
export declare const CloudUploadIcon: ({ fillColor, strokeColor, size, }: CloudUploadIconProps) => JSX.Element;
export {};
